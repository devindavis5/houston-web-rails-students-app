<h1><%=@student.name%></h1>
<h2>Major: <%=@student.major%></h2>
<h2>Instructor: <%= link_to @student.instructor.name, instructor_path(@student.instructor)%></h2>

<%= button_to "Edit", { action: "edit" }, method: :get %>
<%= button_to "Delete", { action: "destroy" }, method: :delete %>